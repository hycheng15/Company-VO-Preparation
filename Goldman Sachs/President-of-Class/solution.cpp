#include <iostream>
#include <string>

using namespace std;

int findPresident(int student_num, int song_length) {
    int i = 1;
    vector<bool> students(student_num, true);
    int remaining_student = student_num;
    while (remaining_student != 1) {
        
        remaining_student -= 1;
    }
}
int main() {

    return 0;
}